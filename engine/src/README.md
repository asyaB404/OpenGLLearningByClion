# OpenGL 学习项目 - 目录结构说明

## 📁 目录结构

```
engine/src/
├── main.cpp                    # 主入口：选择运行哪个 lesson
├── lesson1/                    # Lesson 1: 创建窗口和基本渲染循环
│   └── lesson1.cpp             # Lesson 1 的实现（函数名：lesson1_main）
├── lesson2/                    # Lesson 2: 绘制第一个三角形
│   └── lesson2.cpp             # Lesson 2 的实现（函数名：lesson2_main）
└── README.md                   # 本说明文档
```

## 🚀 使用方法

### 运行程序

1. 编译项目（在 CLion 中点击运行，或使用命令行）
2. 程序启动后会显示菜单
3. 输入数字选择要运行的 lesson：
   - `1` - 运行 Lesson 1（创建窗口和基本渲染循环）
   - `2` - 运行 Lesson 2（绘制第一个三角形）
   - `0` - 退出程序

### 添加新的 Lesson

1. 在 `src/` 目录下创建新文件夹，例如 `lesson3/`
2. 在 `lesson3/` 中创建 `lesson3.cpp`，实现 `lesson3_main()` 函数
   - **注意**：函数名必须是 `lesson3_main()`，不要写成 `main()`
3. 在 `CMakeLists.txt` 中添加新文件：
   ```cmake
   add_executable(OpenGLLearning
       engine/src/main.cpp
       engine/src/lesson1/lesson1.cpp
       engine/src/lesson2/lesson2.cpp
       engine/src/lesson3/lesson3.cpp  # 添加新文件
   )
   ```
4. 在 `main.cpp` 中添加菜单选项和函数声明：
   ```cpp
   // 声明
   extern int lesson3_main();
   
   // 在菜单中添加
   case 3:
       std::cout << "\n>>> 运行 Lesson 3...\n" << std::endl;
       lesson3_main();
       break;
   ```

## 📚 各 Lesson 说明

### Lesson 1: 创建窗口和基本渲染循环
- 学习如何初始化 GLFW 和 GLAD
- 创建 OpenGL 窗口
- 实现基本的渲染循环
- 处理用户输入（ESC 键退出）

### Lesson 2: 绘制第一个三角形
- 学习着色器（Shader）的使用
- 创建和使用顶点缓冲区（VBO）
- 创建和使用索引缓冲区（EBO）
- 创建和使用顶点数组对象（VAO）
- 绘制第一个三角形

## 💡 提示

- 每个 lesson 都是独立的，可以单独运行
- 建议按顺序学习，后面的 lesson 会用到前面的知识
- 每个 lesson 的代码都有详细的中文注释
- 遇到问题可以查看 `GLFW和GLAD说明.md` 文档

## 🔧 编译说明

项目使用 CMake 构建系统，确保：
- CMake 版本 >= 3.15
- C++ 标准 >= C++20
- 已正确配置 GLFW 和 GLAD（在 `engine/3rdparty/` 目录下）

